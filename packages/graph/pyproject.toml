[project]
name = "evident-graph"
version = "0.1.0"
description = "Topic graph construction from resolved memory."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["evident_graph"]

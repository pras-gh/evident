[project]
name = "evident-retrieval"
version = "0.1.0"
description = "Embeddings and vector search over evidence spans."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["evident_retrieval"]

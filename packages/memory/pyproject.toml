[project]
name = "evident-memory"
version = "0.1.0"
description = "The CompanyMemory model, cross-document resolution, and memory cards."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["evident_memory"]
